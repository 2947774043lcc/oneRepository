<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yuntu.mapper.bill.BillMapper">

    <resultMap id="BillList" type="Bill">
         <id property="id" column="id"/>
        <result property="billCode" column="billCode"/>
        <result property="productName" column="productName"/>
        <result property="productDesc" column="productDesc"/>
        <result property="productUnit" column="productUnit"/>
        <result property="productCount" column="productCount"/>
        <result property="totalPrice" column="totalPrice"/>
        <result property="providerId" column="providerId"/>
        <result property="providerId" column="providerId"/>
        <result property="createdBy" column="createdBy"/>
        <result property="creationDate" column="creationDate"/>
        <result property="modifyDate" column="modifyDate"/>
        <result property="modifyBy" column="modifyBy"/>
    </resultMap>
<select id="getBillCount" resultType="int">
    select count(id) as count from smbms_bill
    <trim prefix="WHERE" prefixOverrides="and|or" suffixOverrides="and|or">
    <if test="billCode != null and billCode != ''">
        and billCode like concat('%',#{billCode},'%')
    </if>
    <if test="productName != null and productName != ''">
        and productName like concat('%',#{productName},'%')
    </if>
    </trim>
</select>

    <select id="getBillList" resultMap="BillList">
        select * from smbms_bill
        <trim prefix="WHERE" prefixOverrides="and|or" suffixOverrides="and|or">
        <if test="billCode != null and billCode != ''">
            and billCode like concat('%',#{billCode},'%')
        </if>
        <if test="productName != null and productName != ''">
            and productName like concat('%',#{productName},'%')
        </if>
        </trim>
        order by creationDate DESC  limit #{currentPageNo},#{pageSize}
    </select>

    <insert id="add" parameterType="Bill">
        insert into smbms_bill (billCode,productName,productDesc,
                productUnit,productCount,totalPrice,isPayment,providerId,createdBy,creationDate)
                values(#{billCode},#{productName},#{productDesc},#{productUnit},#{productCount},
                       #{totalPrice},#{isPayment},#{providerId},#{createdBy},#{creationDate})
    </insert>
    <select id="getBillById" resultMap="BillList">
        select * from smbms_bill where 1 = 1 and id = #{id}
    </select>

    <update id="updateBill">
        update smbms_bill set productName=#{productName},productDesc=#{productDesc},
                productUnit=#{productUnit},productCount=#{productCount},totalPrice=#{totalPrice},isPayment=#{isPayment},providerId=#{providerId},modifyBy=#{modifyBy},modifyDate=#{modifyDate} where id = #{id}
    </update>

    <delete id="deleteBillById">
        delete from smbms_bill where id = #{id}
    </delete>
</mapper>