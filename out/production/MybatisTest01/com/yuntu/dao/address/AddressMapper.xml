<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yuntu.dao.address.AddressMapper">
    <select id="getAddressList" resultType="Address">
        select * from smbms_address
    </select>
    <insert id="addAddress" parameterType="Address">
        insert into smbms_address() values ()
    </insert>
    <update id="updateAddress" parameterType="Address">
        update smbms_address set where
    </update>
    <delete id="deleteAddress" parameterType="Long">
    delete from smbms_address where id=#{id}
    </delete>
    <insert id="batchInsertAddress" parameterType="list">

    </insert>
</mapper>
